const domain = 'http://localhost:5000';

export const FINE_INPUT_FORMAT = `${domain}/fine/inputFormat`;
export const FINE_CRUD = `${domain}/fine`;
export const CREW_INPUT_FORMAT = `${domain}/crew/inputFormat`;
export const CREW_CRUD = `${domain}/crew`;
export const WAGE_INPUT_FORMAT = `${domain}/wage/inputFormat`;
export const WAGE_CRUD = `${domain}/wage`;
export const WAGE_RAW = `${domain}/wage/raw`;

export const PROJ_CRUD = `${domain}/proj`;
export const PROJ_CONTENT = `${domain}/proj/content`;
export const PROJ_RICHTEXT = `${domain}/proj/richtext`;
export const PROJ_PIC = `${domain}/proj/pic`;

export const IMG_UPLOAD = `${domain}/img/upload`;

export const IMG_PREFIX = `${domain}/static/images/`

